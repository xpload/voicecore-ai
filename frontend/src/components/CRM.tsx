import React from 'react';
import { Box, Typography } from '@mui/material';

const CRM: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        CRM
      </Typography>
      <Typography variant="body1">
        CRM management interface - to be implemented
      </Typography>
    </Box>
  );
};

export default CRM;
